import {atom} from 'recoil';

export const page1PropState = atom({
  key: 'page1PropState',
  default: '',
});

export const page2PropState = atom({
  key: 'page2PropState',
  default: '',
});

export const page3PropState = atom({
  key: 'page3PropState',
  default: '',
});
